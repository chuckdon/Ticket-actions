# Navigate to the repository's root directory
cd /path/to/your/repository

# Create the shell script file (run-semantic-release.sh)
echo "npx semantic-release" > run-semantic-release.sh

# Make the script executable
chmod +x run-semantic-release.sh

